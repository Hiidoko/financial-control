{
  "name": "financial-future-simulator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start:server": "npm run dev --prefix server",
    "start:client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
