{
  "name": "financial-future-simulator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run start --prefix server",
    "start:full": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start:server": "npm run dev --prefix server",
    "start:client": "npm run dev --prefix client",
  "build": "npm run build --prefix client",
  "serve": "npm run start --prefix server",
  "build:and:start": "npm run build && npm run serve",
    "postinstall": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
